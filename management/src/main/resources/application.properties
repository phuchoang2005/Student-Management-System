spring.application.name=management

spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${POSTGRES_DB:management}
spring.datasource.username=${POSTGRES_USER:management}
spring.datasource.password=${POSTGRES_PASSWORD:management}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.security.user.name=bosshp2005
spring.security.user.password=bosshp2005
