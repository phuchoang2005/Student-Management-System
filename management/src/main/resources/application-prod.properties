# PM-017: hard-disables the demo-accounts route and its seeder in production
# (04-authentication-authorization.md §8, 06-low-level-design.md §11.4). A Spring Boot
# profile-specific file always wins over the base application.properties default, so this can't be
# forgotten by omission.
app.demo-accounts.enabled=false
