collection:
  get:
    operationId: searchCourses
    tags: [Courses]
    summary: Search / list courses
    description: |
      **Roles:** Registrar, Course Administrator, Student. The Librarian has no course workflow and
      no longer holds a read grant here (`02-component-diagram.md` §4). The catalogue is not personal
      data, so no per-caller scoping applies.
    x-roles: [REGISTRAR, COURSE_ADMINISTRATOR, STUDENT]
    parameters:
      - $ref: '../components/parameters/common.yaml#/SearchQueryParam'
      - $ref: '../components/parameters/common.yaml#/PageParam'
      - $ref: '../components/parameters/common.yaml#/SizeParam'
    responses:
      '200':
        description: A page of matching courses.
        content:
          application/json:
            schema:
              allOf:
                - $ref: '../components/schemas/common.yaml#/PageMeta'
                - type: object
                  required: [content]
                  properties:
                    content:
                      type: array
                      items:
                        $ref: '../components/schemas/course.yaml#/CourseSummary'
      '400':
        $ref: '../components/responses/errors.yaml#/BadRequest'
      '401':
        $ref: '../components/responses/errors.yaml#/Unauthorized'

  post:
    operationId: createCourse
    tags: [Courses]
    summary: Create a course
    description: |
      **Roles:** Course Administrator only.
    x-roles: [COURSE_ADMINISTRATOR]
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas/course.yaml#/CourseCreateRequest'
    responses:
      '201':
        description: Course created.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/course.yaml#/CourseResponse'
      '400':
        $ref: '../components/responses/errors.yaml#/BadRequest'
      '401':
        $ref: '../components/responses/errors.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses/errors.yaml#/Forbidden'
      '409':
        description: courseCode already in use.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/common.yaml#/Error'
            example:
              timestamp: '2026-01-15T09:24:00Z'
              status: 409
              error: Conflict
              message: Course code 'CS101' is already in use.
              path: /api/v1/courses

item:
  get:
    operationId: getCourse
    tags: [Courses]
    summary: Get course detail
    description: |
      **Roles:** Registrar, Course Administrator, Student.

      The enrolled-student roster is *not* part of this response — it is
      `GET /enrollments?courseCode=`, which a Student cannot reach. Embedding it would have handed a
      Student browsing the catalogue the names and email addresses of everyone else taking a course
      (api-specification.md §5 decision #10). This response therefore takes no paging parameters:
      it has no paginated field left.
    x-roles: [REGISTRAR, COURSE_ADMINISTRATOR, STUDENT]
    parameters:
      - $ref: '../components/parameters/common.yaml#/CourseCodePathParam'
    responses:
      '200':
        description: The course record.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/course.yaml#/CourseDetail'
      '400':
        $ref: '../components/responses/errors.yaml#/BadRequest'
      '401':
        $ref: '../components/responses/errors.yaml#/Unauthorized'
      '404':
        $ref: '../components/responses/errors.yaml#/NotFound'

  put:
    operationId: updateCourse
    tags: [Courses]
    summary: Update a course
    description: |
      **Roles:** Course Administrator only. courseCode itself is immutable.
    x-roles: [COURSE_ADMINISTRATOR]
    parameters:
      - $ref: '../components/parameters/common.yaml#/CourseCodePathParam'
    requestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: '../components/schemas/course.yaml#/CourseUpdateRequest'
    responses:
      '200':
        description: Updated course.
        content:
          application/json:
            schema:
              $ref: '../components/schemas/course.yaml#/CourseResponse'
      '400':
        $ref: '../components/responses/errors.yaml#/BadRequest'
      '401':
        $ref: '../components/responses/errors.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses/errors.yaml#/Forbidden'
      '404':
        $ref: '../components/responses/errors.yaml#/NotFound'

  delete:
    operationId: removeCourse
    tags: [Courses]
    summary: Remove a course
    description: |
      **Roles:** Course Administrator only.

      Deletes the course row, then asynchronously publishes `CourseDeleted`, which deletes
      every enrollment referencing this course. The response does not wait for the cascade
      to complete.
    x-roles: [COURSE_ADMINISTRATOR]
    parameters:
      - $ref: '../components/parameters/common.yaml#/CourseCodePathParam'
    responses:
      '204':
        description: Course removed.
      '401':
        $ref: '../components/responses/errors.yaml#/Unauthorized'
      '403':
        $ref: '../components/responses/errors.yaml#/Forbidden'
      '404':
        $ref: '../components/responses/errors.yaml#/NotFound'
